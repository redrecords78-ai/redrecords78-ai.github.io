# Red Record's — PWA du studio

Ce dépôt est la source de vérité de la PWA (redrecords78-ai.github.io) : tarifs, instrus, playlist, réservation, dashboard admin, espace équipe. Déploiement automatique sur la branche `main`.

## Avant de répondre : lire le contexte Drive

Au début de CHAQUE session sur ce dépôt, si le connecteur Google Drive est disponible, va chercher le contexte à jour de Ridwane avant de faire quoi que ce soit d'autre — ne demande pas confirmation, c'est automatique :

1. Dossier `CLAUDE` à la racine de son Drive (parentId `17JUCfc0FGCjcO0eYG7D2HO17jwSfwmHE`).
2. Lis `CONTEXTE-RED-RECORDS.pdf` dedans — identité, protocole de travail, état des projets, comptes techniques.
3. Lis `SUIVI CLIPS — Red Record's` — planning et fiches de tournage en cours.
4. Regarde le dossier `JOURNAL MOBILE` (parentId `1HMgrhcW1jYoxGJX3v7e2XURd3Q-lspfe`) — les entrées du jour (`JOURNAL MOBILE - AAAA-MM-JJ`) contiennent ce qui a été décidé côté téléphone depuis la dernière session ; en cas de contradiction avec le PDF, l'entrée la plus récente fait foi.

Si le connecteur Drive n'est pas disponible dans la session, dis-le explicitement puis continue avec ce qui est accessible (dépôt + ce fichier) plutôt que de bloquer.

## La commande « Mis à jour »

Quand Ridwane tape "Mis à jour" : lis les documents ci-dessus (le récent seulement, jamais tout relire en détail), puis résume en français où en sont ses projets. "Mis à jour sur \<projet\>" restreint la lecture à ce seul projet — ne pas élargir de soi-même.

## Protocole de travail (résumé — le détail complet vit dans le CONTEXTE Drive, ne pas le dupliquer ici)

- Propose avant de produire ; une seule référence avant de décliner en série.
- Réponses courtes sur les livrables simples — le contexte va dans les documents, pas dans le chat.
- Guidage étape par étape pour toute manipulation technique.
- Corrige son orthographe d'office sur tout texte publié (titres, PWA, descriptions).
- Vérifie avant de supprimer ou d'écraser ; les suppressions, c'est lui qui tranche.
- Langue : français, toujours. Niveau technique : vocabulaire correct, pas de sursimplification.
